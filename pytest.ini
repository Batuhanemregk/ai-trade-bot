[pytest]
addopts = -q -ra --disable-warnings
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
asyncio_mode = auto
markers =
    core: core unit tests for execution & adapters
    glue: telegram/scheduler glue tests
    slow: optional slow tests
